const db = require('../models');
const Division = db.division;
// const Op = db.Sequelize.Op;

// Add new division
// exports.create = (req, res) => {
//   console.log("Request : ", req.body)
//   validateRequest(req);

//   const division = {
//     name: req.body.name,
//   };

// //   Division.create(division)
// //     .then(data => {
// //       res.send(data);
// //     })
// //     .catch(err => {
// //       res.status(500).send({
// //         message:
// //           err.message || "Error when add a division!"
// //       });
// //     });
// };
